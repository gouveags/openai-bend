{
  "name": "openai-bend",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "An unofficial OpenAI SDK for Bend, with typed requests, streaming, and tool calling.",
  "scripts": {
    "bridge": "bun bridge/cli.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write bridge tests scripts package.json tsconfig.json README.md",
    "format:check": "prettier --check bridge tests scripts package.json tsconfig.json README.md"
  },
  "dependencies": {
    "openai": "7.20.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "typescript": "7.0.2",
    "prettier": "3.9.8"
  }
}
